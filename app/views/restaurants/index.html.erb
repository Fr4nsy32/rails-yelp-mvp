<ol>
  <% @restaurants.each do |rest| %>
    <li><%= link_to rest.name, restaurant_path(rest) %>
    <%= rest.address %>
    <%= rest.phone_number %>
    <%= rest.category %></li>
  <% end %>
</ol>

<%= link_to "Create a new restaurant", new_restaurant_path %>
